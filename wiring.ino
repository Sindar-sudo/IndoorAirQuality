/*

  Screen
  GND > GND
  VCC > 3.3V
  SCL > 14
  SDA > 13
  RES > RST
  DC > 2
  CS > 26
  BLK > 17 for backlight control

  DHT22
  GND > GND
  VCC > VIN (3.3V causes too many nan readings);
  out > 4

  CCS811
  VCC > 3.3V
  GND > GND
  SCL > SCL
  SDA > SDA
  WAK > GND

*/
